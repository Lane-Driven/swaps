# Unreleased

#### Deprecated
> - `~/.local/nvim/nvim-vim-pack/swaps` directory is deprecated and will no longer be checked in a future release.  Use `~/.local/nvim/nvim/swaps instead.

# v0.1.0
> - Development release
> - Supports -d diff, -r remove, -A delete all
